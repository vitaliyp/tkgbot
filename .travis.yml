language: python

sudo: required

dist: xenial

python:
    - 3.7

install:
    - pip install pipenv
    - pipenv install --dev

    - pip install codecov
    - pip install pytest-cov

script:
    - python -m pytest --cov=./tkgbot

after_success:
    - codecov
